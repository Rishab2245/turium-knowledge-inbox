{
  "name": "turium-knowledge-inbox",
  "version": "1.0.0",
  "private": true,
  "description": "A minimal AI Knowledge Inbox: save notes and URLs, then ask questions answered from them with cited sources.",
  "workspaces": [
    "server",
    "web"
  ],
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "dev": "concurrently --names api,web --prefix-colors cyan,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "test": "npm run test --workspace server && npm run test --workspace web",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web",
    "start": "npm run start --workspace server",
    "test:api": "npm run test --workspace server",
    "test:web": "npm run test --workspace web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
